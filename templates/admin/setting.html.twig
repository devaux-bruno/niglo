{% extends 'base.html.twig' %}


{% block body %}
    <h2 class="text-center mt-5 mb-5">Paramètres du site</h2>
    <div class="container">
        <div class="row justify-content-center border">
            <h3 class="col-12 text-center mt-2">Menu Service et camions</h3>
            <div class="form-group col-12 col-sm-12 col-md-12 col-lg-6 col-xl-6 text-center">
                {{ form_start(formAdminEdit) }}
                <div class="my-custom-class-for-errors alert-danger">
                    {{ form_errors(formAdminEdit) }}
                    <span class="text-danger">{{ form_errors(formAdminEdit.publier) }}</span>
                </div>
                <div class="row justify-content-center mt-2">
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xl-6">
                        {{ form_row(formAdminEdit.publier) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 justify-content-center">
                        {{ form_rest(formAdminEdit) }}
                        {{ form_end(formAdminEdit) }}
                    </div>
                </div>
            </div>
            <div class="form-group col-12 col-sm-12 col-md-12 col-lg-6 col-xl-6 text-center">
                {{ form_start(formAdminEdit1) }}
                <div class="my-custom-class-for-errors alert-danger">
                    {{ form_errors(formAdminEdit1) }}
                    <span class="text-danger">{{ form_errors(formAdminEdit1.publier) }}</span>
                </div>
                <div class="row justify-content-center mt-2">
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xl-6">
                        {{ form_row(formAdminEdit1.publier) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 justify-content-center">
                        {{ form_rest(formAdminEdit1) }}
                        {{ form_end(formAdminEdit1) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="row justify-content-center mt-3 border">
            <h3 class="col-12 text-center mt-2">Les partenaires</h3>
            <div class="form-group col-12 col-sm-12 col-md-12 col-lg-6 col-xl-6 text-center">
                {{ form_start(formAdminEdit3) }}
                <div class="my-custom-class-for-errors alert-danger">
                    {{ form_errors(formAdminEdit3) }}
                    <span class="text-danger">{{ form_errors(formAdminEdit3.publier) }}</span>
                </div>
                <div class="row justify-content-center mt-2">
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xl-6">
                        {{ form_row(formAdminEdit3.publier) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 justify-content-center">
                        {{ form_rest(formAdminEdit3) }}
                        {{ form_end(formAdminEdit3) }}
                    </div>
                </div>
            </div>
            <div class="form-group col-12 col-sm-12 col-md-12 col-lg-6 col-xl-6 text-center">
                {{ form_start(formAdminEdit4) }}
                <div class="my-custom-class-for-errors alert-danger">
                    {{ form_errors(formAdminEdit4) }}
                    <span class="text-danger">{{ form_errors(formAdminEdit4.publier) }}</span>
                </div>
                <div class="row justify-content-center mt-2">
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xl-6">
                        {{ form_row(formAdminEdit4.publier) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 justify-content-center">
                        {{ form_rest(formAdminEdit4) }}
                        {{ form_end(formAdminEdit4) }}
                    </div>
                </div>
            </div>
            <div class="row mt-2">
                <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 justify-content-center">
                    <a class="btn btn-secondary mt-2" href="{{ path('home') }}">Retour</a>
                </div>
            </div>
        </div>
    </div>


{% endblock %}